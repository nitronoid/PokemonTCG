#include "tree.h"

/// #include <chrono>
/// #include <thread>
/// std::this_thread::sleep_for(std::chrono::milliseconds(800));

/// build test tree

int main()
{
    Tree test;
    test.createTree();
    test.runTree();

    return 0;
}
